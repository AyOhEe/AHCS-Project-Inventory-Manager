<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Create Listing</title>
    </head>
    <body>
        <h1>Create Listing</h1>
        <form method="post" action="/listing_created">
            <label for="item_name">Item name:</label>
            <input required type="text" id="item_name" name="item_name" value="Unnamed item"><br>
            
            <label for="item_description">Item description:</label><br>
            <textarea name="item_description" rows="10" cols="30">
Write a brief description of the item here.
            </textarea><br>

            <label for="item_category">Item category:</label>
            <select id="item_category" name="item_category">
                {% for key, value in categories.items() %}
                <option value="{{key}}">{{value}}</option>
                {% endfor %}
            </select><br>

            <label for="item_manufacturer">Item manufacturer:</label>
            <select id="item_manufacturer" name="item_manufacturer">
                {% for key, value in manufacturers.items() %}
                <option value="{{key}}">{{value}}</option>
                {% endfor %}
            </select><br>

            <label for="PIN">Employee PIN:</label>
            <input required type="password" id="PIN" name="PIN"><br>

            <input type="submit" value="Submit">
        </form><br>
        <a href="/">Or, return to the homepage</a>
    </body>
</html>