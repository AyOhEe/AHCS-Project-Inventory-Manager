<!DOCTYPE html>
<html lang="en" class="py-3 px-3">
    <head>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Search listings</title>
    </head>
    <body>
        <h1>Search listings</h1>
        Results will only include listings which match the category, manufacturer, and contain the text specified in their name.
        <form method="get" action="/search_results">
            <label for="item_name">Item name:</label>
            <input type="text" id="item_name" name="item_name" value=""><br>

            <label for="item_category">Item category:</label>
            <select id="item_category" name="item_category">
                <option value="-1">Any Category</option>
                {% for key, value in categories.items() %}
                <option value="{{key}}">{{value}}</option>
                {% endfor %}
            </select><br>

            <label for="item_category">Item manufacturer:</label>
            <select id="item_manufacturer" name="item_manufacturer">
                <option value="-1">Any Manufacturer</option>
                {% for key, value in manufacturers.items() %}
                <option value="{{key}}">{{value}}</option>
                {% endfor %}
            </select><br>
            
            <input type="submit" value="Submit">
        </form><br>
        <a href="/">Or, return to the homepage</a>
    </body>
</html>